# GPT-6 Astra 全面审核

审核日期：2026-09-11。范围：文字解析、记账与行程生命周期、统计、前端离线与 GPS、导出实现、部署访问边界。仅做只读生产检查和隔离测试，未修复或部署。

结论：不建议将当前版本作为无人区唯一记账渠道。正常流程可以运行，但存在可复现的离线丢账和金额误记问题。现有测试通过不代表这些边界已覆盖。

## 已确认问题

P1 表示应优先修复的数据安全或金额正确性问题；P2 表示特定条件下的错误或访问边界问题。

| 优先级 | 问题与复现 | 代码位置 | 建议 |
|---|---|---|---|
| P1 | 同步 A 等待网络时新增离线 B，A 成功后队列由 `[A,B]` 变为空；B 未上传却被覆盖 | static/app.js:184–192 | 从最新队列按成功记录 ID 删除；同步互斥 |
| P1 | 本地存储抛额度不足异常，队列未写入，仍清空输入并提示“已安全保存” | static/app.js:38、425–426 | 写入失败向上传递，确认持久化后再清表单 |
| P1 | “在兰州吃饭花了1,200元”解析为200元并允许入账 | app.py:107–141 | 正确处理千分位；不能确认金额时阻止保存 |
| P1 | “在兰州午餐30元，晚餐50元”仅保存50元，无多笔提醒 | app.py:135–141、453–482 | 单笔模式检测多个消费事件并要求拆分，不能默默丢弃 |
| P2 | 快速提交两次产生不同 client_id；离线同步强制确认重复，造成同笔消费重复入账 | static/app.js:189、410–417 | 提交锁与草稿固定 ID，避免无条件绕过重复确认 |
| P2 | 在记录10请求GPS，切换记录11后旧回调返回，覆盖记录11地点 | static/app.js:357–366 | GPS 回调与编辑会话绑定，切换后旧请求失效 |
| P2 | “买汽油添加剂80元”及“今天没加油，在兰州吃饭30元”均被归为油费；“防晒衣”归日用，“一瓶啤酒”无法自动归类 | app.py:175–218 | 排除否定动作与商品词，扩展真实购买语义；低置信度需确认 |
| P2 | 1000出发，1100加10L，次日再加20L但未填里程，输出30L/100km且状态realtime；分子时间比里程分母更新 | app.py:795–817 | 显示里程过期/缺失并要求补充，或只统计完整区间 |
| P2 | 创建/结束行程接受字符串 nan，SQLite 将其变为 NULL；可产生缺出发里程或缺结束里程的行程 | app.py:495–511、751–765 | 与修改里程接口一致，检查有限数字及类型 |
| P2 | 容器18080监听IPv4/IPv6全部接口，应用无登录/接口权限；可连此端口的设备可访问账本。临时服务也接受携带外部Origin的text/plain写请求 | compose.zspace-offline.yaml:8–9；app.py:927–929、959–1023 | 按Tailscale HTTPS使用范围收紧端口，并增加写请求来源/权限校验 |

## 验证证据与边界

- 66项现有单元测试全部通过；20条现有口语矩阵全部通过；前端JS语法检查通过。
- 离线与GPS四项使用Node VM执行原函数/监听器隔离复现。
- 里程与统计边界使用临时SQLite数据库复现；HTTP权限检查仅使用本机临时服务和临时数据库。
- 极空间容器healthy，生产SQLite integrity_check返回ok；已只读核对记录数量与汇总，具体账务数据不随代码发布。
- 生产app.py与本地SHA256一致：ea65856ed9ecc5209880f66637b4517643460651bb9f61c147c02a17b374b374。
- 没有证据表明生产现有账目已经发生上述丢失或误记。
- 未进行真实Android系统断网/杀进程/存储满测试；没有从公网进行可达性探测，不能据全接口监听直接断言公网暴露。
- Excel源码包含地区、经纬度、精度和消费明细；已有测试通过。本次没有使用桌面Excel逐表人工验收，也未进行真实备份恢复演练。

## 口径与功能限制

- 实时油耗按累计购油估算，这是此前约定的口径，不能当成精确油箱耗油。无里程新加油导致统计区间错位是另一个需要修复的问题。
- 当前用车成本按油费、ETC、停车费计算；维修未纳入是现有口径，不在本次擅自调整。
- 退款与押金被明确拒绝，尚不支持；多笔一句话则缺少同样明确的拒绝机制。
- 备份恢复、真机长时间离线、浏览器被系统清理后的恢复能力仍需专门验收。

建议修复顺序：离线持久化与队列合并 → 金额及多笔识别 → 防重复 → 里程边界与统计状态 → GPS会话和分类 → 访问边界。此报告不代表已实施修复。
