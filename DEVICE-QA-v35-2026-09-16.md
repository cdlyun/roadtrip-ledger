# v35 真机复测（本轮已结束，验收未通过）

设备：用户 vivo X Fold6，Android Chrome，通过已有 USB 调试及投屏；服务器前端资源 v35。

## 实测视口

- 外屏：344 × 683 CSS px，DPR 3.25。
- 展开内屏：711 × 584 CSS px，DPR 3.25。浏览器标签栏与系统任务栏可见。
- 内屏实测不是此前浏览器回归假设的 720px；720px 起的媒体查询未命中。

## 已验证

- Tailscale 恢复连接后 v35 样式和脚本正常加载。此前 Clash Meta 占用 VPN，手机健康接口失败，旧离线资源不能作为新版本验收。
- 外屏首页行程入口可进入行程；修改行程表单可打开和取消。
- 外屏新增里程、统计页直接记录里程、弹层关闭可操作。
- 结束区域默认收起，空结束里程与未勾选确认时按钮禁用；没有执行结束。
- 待处理入口显示“当前没有待处理记录”，不触发导出；回收站可展开，仅查看操作按钮，没有恢复或删除记录。
- 餐饮测试句通过 Android 调试输入完整中文后自动填入 meal、30、广元、米粉。
- 加油测试句自动填入 fuel、460、大柴旦中国石化、98、45、33500；实付单价显示 10.222 元/升。
- 展开内屏时上述六个字段全部保留，弹层仍打开，待同步数量为 0。滚动位置随重排从约474变为420px，不声称像素位置严格保持。

## 未通过 / 需修复

1. P0：内屏宽 711px 未触发 720px 双栏规则，首页、行程、账单、统计均保持纵向堆叠，底部导航仍为横栏。不是“宽屏双栏已完成”；修复应按实际可用宽度及容器最小可读宽度设计，不能按设备名称判断。补711及断点边界回归。
2. 表单字段提示暴露 category_label、people、nights、note 等内部英文键；餐饮仍显示油号等无关待补充项，造成误导。
3. 弹层向下滚动时标题和关闭按钮离开视野，需要滚回顶部才能点关闭。
4. 自动识别切到确认表单后缺少明显的“返回修改原句/重新识别”入口；关闭再打开仍停留确认表单。刷新后才易于找到丢弃草稿入口。

## 边界和待办

- 本轮仅创建未提交测试草稿，未入账、未删改生产记录、未修改网络设置。
- 投屏的粘贴/中文键入通道出现漏字，不能将不完整输入的识别结果归因给解析器；改用 Android CDP Input.insertText 验证完整中文。
- 当前无有效消费和独立里程记录，真实记录修改/删除流程未在生产执行；之前隔离浏览器的 PUT/POST 测试不冒充真机验证。
- 折回外屏后实测344×683；仍在stats页面，record弹层仍打开，fuel/460/大柴旦中国石化/98/45/33500六字段完全保留，待同步0。已核对测试原句后仅丢弃本轮草稿，确认draftRemaining=false、queue=0，关闭弹层并目视返回统计页。没有提交、修改、恢复或删除已有账单。
- iPhone、软键盘遮挡、GPS权限、离线入账及Excel下载本轮未验收。
