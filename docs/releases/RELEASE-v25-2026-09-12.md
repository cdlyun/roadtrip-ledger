# v25 修复与验收

2026-09-12，Terra 实现，Astra 独立终审及部署验收。

修复范围：离线队列同步覆盖、存储失败误报成功、跨标签并发写入、固定草稿编号与防重复提交、重复队列确认入口、旧队列编号迁移、GPS会话隔离及按钮恢复、千分位金额、多金额混合消费拦截、否定加油与商品分类、缺最新里程的油耗/每公里成本提示、有限里程校验、接口来源校验及端口收紧。

验证：69项Python测试、20条既有口语矩阵、Node离线回归、JS语法检查通过。Astra独立执行真实前端监听器模拟，确认存储失败保留草稿、并发双击仅一次提交、GPS旧回调不覆盖新表单、两个VM共用串行锁保留两个追加记录、损坏队列拒绝覆盖。最终单价反例“加95号汽油340元40升，单价8块5每升”金额340、可保存。

极空间：容器healthy；首页v25；端口仅127.0.0.1:18080；原Tailscale HTTPS同源解析成功，外部Origin解析请求400拒绝。多消费与口语单价线上解析检查通过。行程与账目逐行等于部署前备份，记录数量及汇总一致。Excel在线导出的ZIP/XML结构检查通过。

备份（NAS应用目录内）：data/backups/before-v25-20260912.db；backups/source-before-v25-20260912.tar.gz。数据库备份使用SQLite backup API，完整性检查ok。

边界：仍使用tailnet访问边界，无独立账户系统；离线安全写入要求浏览器支持Web Locks，不支持时明确拒绝并保留输入。未进行真实Android存储满、系统杀进程及长期离线实机测试。规则解析不能保证任意自然语言正确，保存前仍需核对；多笔消费需拆开录入。原审核报告保留为历史问题记录。
