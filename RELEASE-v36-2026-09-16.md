# V3.1 前端修复 v36

## 变更

- 基于 viewport/container 宽度，在 648–991 CSS px 启用双栏与 88px 侧栏，覆盖实测 711×584 内屏；不按设备名称判断。
- 字段提示使用中文并按消费分类过滤，区分必填、影响统计和可选项。
- 表单关闭区域固定可见；支持修改原句及重新识别，并保护手动修改、草稿、刷新和异步返回状态。
- 静态资源及离线缓存升级为 v36；没有数据库迁移或统计口径变更。

## 验证

- 前端回归通过。
- 完整浏览器回归通过：10 个 viewport、4 个页面，共 40 项页面/宽度检查；另覆盖触控尺寸、短屏关闭、草稿折叠连续性、重识别取消/失败/刷新、晚响应、保存中保护和里程增改。
- 前序隔离验证：后端 98 项、自然语言 20 项、PWA 10 项缓存资源及断网加载通过。
- 已部署极空间。HTTPS 健康接口正常、容器 healthy，四个部署静态文件 SHA-256 与本地一致。
- 自动化使用隔离临时数据库，没有写入生产账单。

## 尚未验证

- 本版仍需 vivo 外屏/内屏真机复测；浏览器验证不替代真机验收。
- GitHub 上传需配置用户仓库及认证，不能将本发布记录理解为已上传。

## 回退

部署前保留了 v35 静态文件备份；回退只恢复前端静态资源并重建容器，不覆盖数据目录。
